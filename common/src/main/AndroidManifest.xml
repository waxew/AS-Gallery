<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
        AS Gallery editions no longer include Firebase SDKs, so no provider override is required.
        Keeping this manifest intentionally minimal avoids false Firebase merge warnings.
    -->
</manifest>
