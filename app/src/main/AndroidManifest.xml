<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- برای بررسی نسخه جدید فقط متادیتای GitHub Releases خوانده می‌شود. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Required to provide app-lock feature. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <!-- suppress DeprecatedClassUsageInspection -->
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />

    <!-- Devices running Android 12L (API level 32) or lower. -->
    <!-- suppress ScopedStorage -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Devices running Android 13 (API level 33) or higher. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <!-- Android 14+ limited/selected visual media access. -->
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_launcher_foreground"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_foreground"
        android:supportsRtl="true"
        android:theme="@style/Theme.Splash"
        tools:targetApi="31">

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">

            <!-- AnalyticsInitializer در نسخه AS روی پیاده‌سازی no-op قرار دارد. -->
            <meta-data
                android:name="com.zs.gallery.impl.AnalyticsInitializer"
                android:value="androidx.startup" />

            <!-- Dependency injection. -->
            <meta-data
                android:name="com.zs.gallery.impl.KoinInitializer"
                android:value="androidx.startup" />

            <!-- Shared Coil image loader. -->
            <meta-data
                android:name="com.zs.gallery.impl.CoilInitializer"
                android:value="androidx.startup" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|screenLayout"
            android:exported="true"
            android:launchMode="singleTask">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Open images/videos shared from camera, file manager and other apps. -->
            <intent-filter android:label="@string/app_name">
                <action android:name="android.intent.action.VIEW" />
                <action android:name="com.android.camera.action.REVIEW" />
                <action android:name="android.provider.action.REVIEW" />
                <action android:name="android.provider.action.REVIEW_SECURE" />

                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="image/*" />
                <data android:mimeType="video/*" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
            </intent-filter>

            <!-- JPEG-XL fallback matching for file managers that do not expose image/jxl. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="com.android.camera.action.REVIEW" />
                <action android:name="android.provider.action.REVIEW" />
                <action android:name="android.provider.action.REVIEW_SECURE" />

                <data android:scheme="content" />
                <data android:scheme="http" />
                <data android:scheme="file" />
                <data android:mimeType="image/jxl" />
                <data android:host="*" />
                <data android:mimeType="*/*" />
                <data android:pathPattern=".*\\.jxl" />
                <data android:pathPattern=".*\\..*\\.jxl" />
                <data android:pathPattern=".*\\..*\\..*\\.jxl" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\.jxl" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
            </intent-filter>
        </activity>
    </application>
</manifest>
